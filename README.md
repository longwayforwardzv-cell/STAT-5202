It only requires a few packages.
Place the horse racing data CSV file in the project folder, and the script will automatically generate predictions for the exam sheet.
If the labels in the test set are removed, the script will output predictions for the test set as well.
